---
类型: 单词索引
标题: 单词字母索引
---

# 📖 单词字母索引

> 按字母顺序快速查找单词卡片

---

## A - E

```dataview
LIST
FROM "单词库"
WHERE 类型 = "单词卡片" AND regexmatch("(?i)^[a-e]", 拼写)
SORT 拼写 ASC
```

## F - J

```dataview
LIST
FROM "单词库"
WHERE 类型 = "单词卡片" AND regexmatch("(?i)^[f-j]", 拼写)
SORT 拼写 ASC
```

## K - O

```dataview
LIST
FROM "单词库"
WHERE 类型 = "单词卡片" AND regexmatch("(?i)^[k-o]", 拼写)
SORT 拼写 ASC
```

## P - T

```dataview
LIST
FROM "单词库"
WHERE 类型 = "单词卡片" AND regexmatch("(?i)^[p-t]", 拼写)
SORT 拼写 ASC
```

## U - Z

```dataview
LIST
FROM "单词库"
WHERE 类型 = "单词卡片" AND regexmatch("(?i)^[u-z]", 拼写)
SORT 拼写 ASC
```

## 全部单词

```dataview
TABLE 拼写, 国际音标, 词性分类, 掌握程度
FROM "单词库"
WHERE 类型 = "单词卡片"
SORT 拼写 ASC
```
